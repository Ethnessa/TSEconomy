namespace TSEconomy.Database.Models.Properties
{
    // Todo: potentially add more properties or port some perms to properties but
    // that would make it so some account would give us the job to allow players to remove properties
    public enum BankAccountProperties
    {
        Default,
        WorldAccount
    }
}

